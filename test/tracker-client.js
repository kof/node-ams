module = QUnit.module;

module('tracker-client');

test('test if tracked dependencies are correct', function() {
	/*
	{
		'a.js': ['c.js', 'd.js'],
		'b.js': ['c.js', 'd.js']
	}
	*/
	//same(ams.deps(), );
});